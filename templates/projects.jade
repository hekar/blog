extends layout

block append header
  div.nav
    a(href=locals.url + '/index.html') « Blog

block content
  section.content
    !=contents['projects.md'].html

block prepend footer
  div.nav
    if prevPage
      a(href=prevPage.url) « Newer
    else
      a(href=locals.url + '/archive.html') « Archives
    if nextPage
      a(href=nextPage.url) Next page »


